/**
 * Fixed height (px) of the media tray (pack / GIF / sticker picker) that slides
 * in where the keyboard was. The composer bar is raised by this amount while
 * the tray is open, so opening it never shifts the layout — WhatsApp-style.
 */
export const MEDIA_TRAY_HEIGHT = 288;